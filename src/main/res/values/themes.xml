<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.CustomAr" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">#8AAA96</item>
        <item name="colorSecondary">#D5FE897F</item>
        <item name="android:statusBarColor">#8AAA96</item>
    </style>

    <style name="FloatingHint" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <!-- Set the desired padding between the tabs (adjust the value as needed) -->
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingEnd">0dp</item>
    </style>
    <style name="TextStyleBNV" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">4sp</item>
        <item name="android:padding">0dp</item>
        <item name="textAllCaps">true</item>
    </style>
    <style name="SmallRatingBarStyle" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:progressBackgroundTint">#606361</item>
        <item name="android:progressTint">#F1D237</item>
      <!-- Adjust the maxHeight to control the size -->
    </style>
    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Customize other SearchView attributes as needed -->
        <item name="android:textSize">5sp</item> <!-- Adjust the text size as needed -->
    </style>
    <style name="OtpEditTextStyle">
        <item name="android:layout_width">50dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:textSize">24sp</item>
        <item name="android:gravity">center</item>
        <item name="android:inputType">none</item> <!-- No soft keyboard input -->
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:background">@drawable/otp_edittext_background</item>
    </style>

</resources>